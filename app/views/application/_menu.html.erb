<header>
  <div class="px-3 py-2 text-bg-dark">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <%= render 'shared/header' %>
        <%= render menu.content %>
      </div>
    </div>
  </div>
</header>
<header>
  <%= render 'menu_admin' if menu.admin? %>
</header>
